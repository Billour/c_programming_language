#include <stdio.h>
#include <stdlib.h>

int main()
{

    printf("  If the  answer is correct than screen will print \"1\", if it is incorrect that will print \"0\"   \n");
    printf(" 20 <=20.001   ? %d \n ", 20 <= 20.001);
    printf("'A' == 65 ? %d    \n ", 'A' ==65);
    printf("20!= 20.000001    ? %d  \n ",  20!= 20.000001);
    printf("'A' > 'a'    ? %d  \n ", 'A' > 'a');

    system("pause");
    return 0;
}
